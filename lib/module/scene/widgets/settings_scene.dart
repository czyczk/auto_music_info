import 'package:flutter/cupertino.dart';

class SettingsScene extends StatelessWidget {
  const SettingsScene({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Settings Scene');
  }
}
